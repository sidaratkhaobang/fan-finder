version: '3'
services:
  web:
    image: nginx:1.15-alpine
    ports: 
      - 1901:80
    volumes:
      - ./web:/usr/share/nginx/html
  api:
    build:
      context: ./
      dockerfile: _api.Dockerfile
    ports:
      - 1902:80
    volumes:
      - ./api:/var/www/html/
